import VisitDTO from "../../../shared/api/dto/VisitDTO"

export default class VisitService {
    saveVisit(visit: VisitDTO) {
        // get restaurant from db

        // add visit

        // save again through repo
    }
}